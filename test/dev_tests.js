/*
import {Crawler} from "../dist/io.github.alpha-dev.objtemplates/crawler.js";
import {repository_crawl} from "../dist/io.github.alpha-dev.objtemplates/repository_crawl.js";
import {Rule} from "../dist/io.github.alpha-dev.rules/Rule.js";

let test = require('unit.js');

describe('Main Crawler', function(){
  //Tests to make sure the initial crawl runs properly
  it('Test main fetch', function(){
    let crawler = new Crawler("https://api.github.com/search").beginCrawl("repositories?q=tetris+language:assembly&sort=stars&order=desc");
  });
});

describe('Repository Crawler', function(){
  it('Test repository crawler', function(){
    let repoCrawl = new repository_crawl("Alpha-Dev/Recodify", "", "master", new Rule()).getRootFiles();
  });
});
*/
"use strict";